# Forward Networks API Configuration
FORWARD_API_KEY=your_api_key_here
FORWARD_API_SECRET=your_api_secret_here
FORWARD_API_BASE_URL=https://fwd.app

# Instance Configuration (optional)
# If not set, instance ID will be generated from API_BASE_URL
# Use 'list_instance_ids' tool to see available instances
# FORWARD_INSTANCE_ID=49bd3225

# Default Network Settings - Set these to avoid specifying network_id in every tool call
# Replace with your actual network ID (use list_networks to find it)
FORWARD_DEFAULT_NETWORK_ID=162112
FORWARD_DEFAULT_QUERY_LIMIT=100

# Optional: Default snapshot ID (leave empty to always use latest)
# FORWARD_DEFAULT_SNAPSHOT_ID=

# ⚠️ TLS Configuration - SECURITY CRITICAL
# Skip TLS certificate verification (DANGEROUS - only use for development with self-signed certs)
# SECURITY WARNING: Setting this to 'true' disables certificate validation and makes you vulnerable
# to man-in-the-middle attacks. Only enable in isolated development environments.
# Production systems MUST keep this set to 'false'.
FORWARD_INSECURE_SKIP_VERIFY=false

# Optional: Custom CA certificate file path
# FORWARD_CA_CERT_PATH=/path/to/ca-certificate.pem

# Optional: Client certificate and key for mutual TLS authentication
# FORWARD_CLIENT_CERT_PATH=/path/to/client-certificate.pem
# FORWARD_CLIENT_KEY_PATH=/path/to/client-private-key.pem

# API timeout in seconds
FORWARD_TIMEOUT=30

# 🧠 Semantic Cache Configuration (AI-powered query optimization)
# Enable semantic caching for NQE queries (significantly improves performance)
FORWARD_SEMANTIC_CACHE_ENABLED=true

# Maximum number of cached query results
FORWARD_SEMANTIC_CACHE_MAX_ENTRIES=1000

# Time-to-live for cache entries in hours
FORWARD_SEMANTIC_CACHE_TTL_HOURS=24

# Similarity threshold for semantic matching (0.0-1.0, higher = more strict)
FORWARD_SEMANTIC_CACHE_SIMILARITY_THRESHOLD=0.85

# Embedding service provider (openai, keyword, or mock)
FORWARD_EMBEDDING_PROVIDER=keyword

# 💾 Enhanced Cache Configuration for Large API Results
# Maximum memory usage for cache in MB (default: 512MB)
FORWARD_SEMANTIC_CACHE_MAX_MEMORY_MB=512

# Cache eviction policy: lru, lfu, ttl, size, oldest, random (default: lru)
FORWARD_SEMANTIC_CACHE_EVICTION_POLICY=lru

# Enable result compression to save memory (default: true)
FORWARD_SEMANTIC_CACHE_COMPRESS_RESULTS=true

# Compression level 1-9 (1=fastest, 9=best compression, default: 6)
FORWARD_SEMANTIC_CACHE_COMPRESSION_LEVEL=6

# Enable disk persistence for very large results (default: false)
FORWARD_SEMANTIC_CACHE_PERSIST_TO_DISK=false

# Disk cache path for persistent storage
FORWARD_SEMANTIC_CACHE_DISK_PATH=/tmp/forward-cache

# Enable detailed cache metrics and statistics (default: true)
FORWARD_SEMANTIC_CACHE_METRICS_ENABLED=true

# Memory usage threshold for eviction (0.0-1.0, default: 0.8 = 80%)
FORWARD_SEMANTIC_CACHE_MEMORY_THRESHOLD=0.8

# Background cleanup interval in minutes (default: 30)
FORWARD_SEMANTIC_CACHE_CLEANUP_INTERVAL=30

# 🔑 OpenAI API Key (required for semantic caching with openai provider)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# MCP Server Configuration (optional)
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# MCP Configuration (optional)
MCP_VERSION=v1
MCP_MAX_RETRIES=3 